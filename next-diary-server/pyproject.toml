[project]
name = "next-diary-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[all]>=0.116.1",
    "pyinstaller>=6.14.2",
    "python-multipart>=0.0.20",
    "sqlmodel>=0.0.24",
]
